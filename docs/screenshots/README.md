# Screenshots

Add marketing captures here before a public launch.

| File | Purpose |
|------|---------|
| `desktop-hero.svg` | README hero placeholder (replace with `desktop-hero.png` or `.gif` when ready) |
| `desktop-hero.png` | Preferred hero asset — workbench with chat, tool cards, inline diff |
| `cli-tui.png` | Optional — `ARCDESK chat` terminal UI |

**Capture tips:** open a real project, run a task that produces a visible tool call + diff,
light and dark mode if possible, no secrets in the frame.
