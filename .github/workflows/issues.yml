name: issues

on:
  push:
    branches: [main]
    paths: [README.md, README.en.md, .github/workflows/issues.yml]

jobs:
  badge:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Open issues at https://github.com/P1ouson/deepseek-ArcDesk/issues"
