/** Matches `--duration-normal` in design-system.css — single JS source for panel timers. */
export const MOTION_DURATION_NORMAL_MS = 220;

/** Matches `--duration-fast` in design-system.css */
export const MOTION_DURATION_FAST_MS = 140;
