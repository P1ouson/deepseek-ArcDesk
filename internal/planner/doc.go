// Package planner provides phased execution for coding tasks (P1-#10):
// parse a markdown plan into ordered phases, gate final answers until each
// phase is explicitly completed, and expose status tools to the agent.
package planner
