// Package rag documents Harness layer 5: structured retrieval tools registered at
// boot (gitrag, archrag, uirag, dependency, callgraph, runtime, failuremem, …).
// Vector embedding search is intentionally out of scope — see docs/maturity/P2-ROADMAP.md.
package rag

const PackageDoc = "harness layer 5 — structured RAG tools, no embedding index"
