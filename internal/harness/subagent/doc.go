// Package subagent documents Harness layer 4: isolated delegation via agent.task,
// skill runAs=subagent, and agent.Coordinator dual-session planning.
package subagent

const PackageDoc = "harness layer 4 — agent.task, skill subagent, Coordinator"
